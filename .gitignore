node_modules/
dist/
build/
.venv/
__pycache__/
*.pyc

# build outputs (audio + PDFs) — large, regenerable
dist/

# voice-renderer fallback dir (when input is outside stories/)
tools/voice-renderer/out/

# pandoc build output if run locally outside dist/
*.pdf

# editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# env
.env
.env.local
