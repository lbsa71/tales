node_modules/
dist/
build/
.venv/
__pycache__/
*.pyc

# build outputs (audio + PDFs) — large, regenerable
dist/

# voice-renderer fallback dir (when input is outside stories/)
tools/voice-renderer/out/

# pandoc build output if run locally
*.pdf

# compiled/ holds intentionally-committed release artifacts — un-ignore
!compiled/
!compiled/**

# editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# env
.env
.env.local
